import { css } from '@emotion/react';

interface Props {}

const Work = ({}: Props) => {
  return (
    <li css={component}>
      <div></div>
    </li>
  );
};

const component = css`
  position: relative;
`;

export default Work;
